/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QuizSubmitRequest } from '../../models/quiz-submit-request';
import { QuizSubmitResponse } from '../../models/quiz-submit-response';

export interface SubmitQuiz$Params {
  quiz_submission_id: number;
      body: QuizSubmitRequest
}

export function submitQuiz(http: HttpClient, rootUrl: string, params: SubmitQuiz$Params, context?: HttpContext): Observable<StrictHttpResponse<QuizSubmitResponse>> {
  const rb = new RequestBuilder(rootUrl, submitQuiz.PATH, 'post');
  if (params) {
    rb.path('quiz_submission_id', params.quiz_submission_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<QuizSubmitResponse>;
    })
  );
}

submitQuiz.PATH = '/api/quizzes/{quiz_submission_id}/submit';
