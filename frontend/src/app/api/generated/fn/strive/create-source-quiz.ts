/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QuizGenerationJobResponse } from '../../models/quiz-generation-job-response';
import { SourceQuizCreateRequest } from '../../models/source-quiz-create-request';

export interface CreateSourceQuiz$Params {
  source_id: number;
      body: SourceQuizCreateRequest
}

export function createSourceQuiz(http: HttpClient, rootUrl: string, params: CreateSourceQuiz$Params, context?: HttpContext): Observable<StrictHttpResponse<QuizGenerationJobResponse>> {
  const rb = new RequestBuilder(rootUrl, createSourceQuiz.PATH, 'post');
  if (params) {
    rb.path('source_id', params.source_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<QuizGenerationJobResponse>;
    })
  );
}

createSourceQuiz.PATH = '/api/sources/{source_id}/quizzes';
