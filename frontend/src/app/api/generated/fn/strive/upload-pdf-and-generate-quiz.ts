/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BodyUploadPdfAndGenerateQuiz } from '../../models/body-upload-pdf-and-generate-quiz';
import { QuizGenerationJobResponse } from '../../models/quiz-generation-job-response';

export interface UploadPdfAndGenerateQuiz$Params {
  activity_id: number;
      body: BodyUploadPdfAndGenerateQuiz
}

export function uploadPdfAndGenerateQuiz(http: HttpClient, rootUrl: string, params: UploadPdfAndGenerateQuiz$Params, context?: HttpContext): Observable<StrictHttpResponse<QuizGenerationJobResponse>> {
  const rb = new RequestBuilder(rootUrl, uploadPdfAndGenerateQuiz.PATH, 'post');
  if (params) {
    rb.path('activity_id', params.activity_id, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<QuizGenerationJobResponse>;
    })
  );
}

uploadPdfAndGenerateQuiz.PATH = '/api/activities/{activity_id}/quizzes/upload-pdf';
